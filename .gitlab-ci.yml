---
stages:
  - init
  - unit_test
  - curl_test
  - deploy

init-test:
  stage: init
  script:
    - echo "import sqlite3" > py_sqlite_test.txt
    - python3 < py_sqlite_test.txt
    - touch testDB.db
    - sqlite3 testDB.db < mkdb.txt

unit-test:
  stage: unit_test
  script:
    - echo "Hello unit-test"

curl-test:
  stage: curl_test
  script: 
    - echo "Hello curl-test"

deploy-app:
  stage: deploy
  script: echo "Hello deploy-app"
