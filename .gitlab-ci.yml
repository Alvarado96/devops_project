---
stages:
  - init
  - unit_test
  - curl_test
  - deploy

init-test:
  stage: init
  script:
    - echo "init stage"

unit-test:
  stage: unit_test
  script:
    - echo "Hello unit-test"

curl-test:
  stage: curl_test
  script: 
    - cd tests/curl_tests/
    - touch properties.db
    - cat ../../mkdb.txt
    #- sqlite3 properties.db < ../../mkdb.txt
    #- python3 ../../main.py -p 12185 &
    #- bash test_driver.sh localhost 12185 $!

deploy-app:
  stage: deploy
  script: echo "Hello deploy-app"
