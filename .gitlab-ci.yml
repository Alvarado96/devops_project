---
stages:
  - build
  - unit_test
  - curl_test
  - deploy

build-app:
  stage: build
  script:
    - echo "Hello build"
    - python3 --version
    - sqlite -version

unit-test:
  stage: unit_test
  script:
    - echo "Hello unit-test"

curl-test:
  stage: curl_test
  script: echo "Hello curl-test"

deploy-app:
  stage: deploy
  script: echo "Hello deploy-app"
